--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -21,6 +21,8 @@ services:
 
   musicbrainz:
     build: musicbrainz-dockerfile
+    ports:
+      - "5000:5000"
     volumes:
       - dbdump:/media/dbdump
     restart: unless-stopped
